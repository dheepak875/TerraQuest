#!/usr/bin/expect -f

set password "letsgoww"
set timeout 30

spawn ssh -o StrictHostKeyChecking=no terraq@TerraQuest-2.local "ls -l /home/terraq/TerraQuest/launch_at_boot.sh; crontab -l"

expect {
    "password:" { send "$password\r"; exp_continue }
    eof
}
puts "\nDiagnostic Complete."
